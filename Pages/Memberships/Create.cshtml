@page
@model prayer.Pages.Memberships.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Membership</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Membership.UserId" class="control-label">User</label>
                <select asp-for="Membership.UserId" class ="form-control" asp-items="ViewBag.UserId">
                    <option value="">-- Select a category --</option>
                </select>

            </div>
            <div class="form-group">
                <label asp-for="Membership.GroupId" class="control-label">Group</label>
                <select asp-for="Membership.GroupId" class ="form-control" asp-items="ViewBag.GroupId">
                    <option value="">-- Select a category --</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
